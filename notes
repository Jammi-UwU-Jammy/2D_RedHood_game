1. Return characters back into normal scaling.
2. Character's Bounding box is currently set to scaling of 1.

Game server -> Does game logic including map loading, collisions, monster's movements, etc.
Client -> receive environments data, render game on local machine, send player's data to server